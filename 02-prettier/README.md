# Prettier - Форматировщик кода

## 📋 Описание

**Prettier** - это opinionated форматировщик кода, который поддерживает множество языков и интегрируется с большинством редакторов. Главная цель - избавить команду от споров о стиле кода.

### Зачем нужен Prettier?

- ⚡ **Мгновенное форматирование** - форматирует код автоматически при сохранении
- 🎨 **Единый стиль** - одинаковый код у всей команды
- 🔧 **Без настроек** - работает из коробки с разумными умолчаниями
- 🌍 **Многоязычность** - JS, TS, HTML, CSS, JSON, Markdown и другие
- 🤝 **Интеграция с ESLint** - работает вместе с линтерами

## 🚀 Установка

### 1. Установить расширение VS Code
- Extensions (`Ctrl+Shift+X`) → **"Prettier - Code formatter"**

### 2. Установить Prettier в проект
```bash
npm install --save-dev prettier
```

## ⚙️ Топ-15 настроек Prettier

### 1. `semi` - Точки с запятой
**Значение:** `true` (по умолчанию)
**Описание:** Добавлять точки с запятой в конце выражений.

### 2. `singleQuote` - Одинарные кавычки
**Значение:** `true`
**Описание:** Использовать одинарные кавычки вместо двойных.

### 3. `printWidth` - Ширина строки
**Значение:** `80` (по умолчанию)
**Описание:** Максимальная длина строки перед переносом.

### 4. `tabWidth` - Размер таба
**Значение:** `2`
**Описание:** Количество пробелов для одного уровня отступа.

### 5. `useTabs` - Использовать табы
**Значение:** `false`
**Описание:** Использовать пробелы вместо табов.

### 6. `trailingComma` - Trailing запятые
**Значение:** `"es5"`
**Описание:** Где добавлять trailing запятые (`none`, `es5`, `all`).

### 7. `bracketSpacing` - Пробелы в объектах
**Значение:** `true`
**Описание:** Пробелы между скобками: `{ foo: bar }` vs `{foo: bar}`.

### 8. `arrowParens` - Скобки в стрелочных функциях
**Значение:** `"always"`
**Описание:** Скобки вокруг единственного параметра (`always`, `avoid`).

### 9. `endOfLine` - Конец строки
**Значение:** `"lf"`
**Описание:** Тип окончания строки (`lf`, `crlf`, `cr`, `auto`).

### 10. `htmlWhitespaceSensitivity` - Пробелы в HTML
**Значение:** `"css"`
**Описание:** Чувствительность к пробелам в HTML.

### 11. `jsxSingleQuote` - Кавычки в JSX
**Значение:** `false`
**Описание:** Использовать одинарные кавычки в JSX атрибутах.

### 12. `proseWrap` - Перенос текста
**Значение:** `"preserve"`
**Описание:** Как переносить Markdown текст.

### 13. `quoteProps` - Кавычки в свойствах объектов
**Значение:** `"as-needed"`
**Описание:** Когда добавлять кавычки к ключам объектов.

### 14. `requirePragma` - Требовать pragma
**Значение:** `false`
**Описание:** Форматировать только файлы с `@prettier` или `@format`.

### 15. `vueIndentScriptAndStyle` - Отступы в Vue
**Значение:** `false`
**Описание:** Делать отступы для `<script>` и `<style>` в Vue файлах.

## 📚 Примеры

### JavaScript: До и После

**До:**
```javascript
const user={name:"John",age:30};
function calc(a,b){return a+b;}
```

**После:**
```javascript
const user = { name: 'John', age: 30 };
function calc(a, b) {
  return a + b;
}
```

📁 Примеры в `examples/basic/` и `examples/advanced/`

## 🔧 Команды

```bash
# Форматировать все файлы
npx prettier --write .

# Проверить без изменений
npx prettier --check .

# Форматировать только JS
npx prettier --write "**/*.{js,jsx,ts,tsx}"
```

## ⌨️ Горячие клавиши

- **Format Document:** `Shift+Alt+F`
- **Format Selection:** выделить код → `Shift+Alt+F`
- **Format on Save:** settings.json:
  ```json
  {
    "editor.defaultFormatter": "esbenp.prettier-vscode",
    "editor.formatOnSave": true
  }
  ```

## 🔄 Интеграция с ESLint

```bash
npm install --save-dev eslint-config-prettier eslint-plugin-prettier
```

`.eslintrc.json`:
```json
{
  "extends": ["plugin:prettier/recommended"]
}
```

## 🔗 Полезные ссылки

- [Официальная документация](https://prettier.io/)
- [Playground](https://prettier.io/playground/)
- [VS Code расширение](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode)

[⬅️ Назад к списку расширений](../README.md)
