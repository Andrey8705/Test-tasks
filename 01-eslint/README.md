# ESLint - Линтер для JavaScript/TypeScript

## 📋 Описание

**ESLint** - это мощный инструмент для статического анализа JavaScript и TypeScript кода. Он находит проблемы, потенциальные баги и несоответствия стилю кодирования, а также может автоматически исправлять многие из них.

### Зачем нужен ESLint?

- 🐛 **Находит ошибки до выполнения** - обнаруживает синтаксические и логические ошибки
- 📏 **Единый стиль кода** - обеспечивает консистентность в команде
- ⚡ **Автоматическое исправление** - исправляет многие проблемы одной командой
- 🔧 **Настраиваемость** - гибкая конфигурация под любой проект
- 🚀 **Интеграция с фреймворками** - поддержка React, Vue, TypeScript и других

## 🚀 Установка

### 1. Установить расширение VS Code

1. Открыть VS Code/Cursor
2. Перейти в Extensions (`Ctrl+Shift+X`)
3. Найти "**ESLint**" (автор: Microsoft)
4. Нажать **Install**

### 2. Установить ESLint в проект

```bash
# С npm
npm install --save-dev eslint

# С yarn
yarn add --dev eslint

# Инициализация конфигурации
npx eslint --init
```

## ⚙️ Топ-15 настроек ESLint

### 1. `no-console` - Предупреждение о console.log

**Параметр:** `"no-console": "warn"`
**Описание:** Предупреждает о забытых console.log в коде. Полезно для production кода.

```javascript
console.log('Debug'); // ⚠️ Warning
```

### 2. `no-unused-vars` - Неиспользуемые переменные

**Параметр:** `"no-unused-vars": ["error", { "argsIgnorePattern": "^_" }]`
**Описание:** Ошибка при объявлении переменных, которые не используются.

```javascript
const unusedVar = 42; // ❌ Error
const _ignoredVar = 42; // ✅ OK (начинается с _)
```

### 3. `eqeqeq` - Строгое сравнение

**Параметр:** `"eqeqeq": ["error", "always"]`
**Описание:** Требует использования `===` и `!==` вместо `==` и `!=`.

```javascript
if (x == 5) // ❌ Error
if (x === 5) // ✅ OK
```

### 4. `prefer-const` - Предпочтение const

**Параметр:** `"prefer-const": "error"`
**Описание:** Требует `const` для переменных, которые не переназначаются.

```javascript
let x = 5; // ❌ Error (если x не переназначается)
const x = 5; // ✅ OK
```

### 5. `no-var` - Запрет var

**Параметр:** `"no-var": "error"`
**Описание:** Запрещает использование `var`, требует `let`/`const`.

```javascript
var x = 5; // ❌ Error
let x = 5; // ✅ OK
```

### 6. `semi` - Точки с запятой

**Параметр:** `"semi": ["error", "always"]`
**Описание:** Требует точки с запятой в конце выражений.

```javascript
const x = 5 // ❌ Error
const x = 5; // ✅ OK
```

### 7. `quotes` - Тип кавычек

**Параметр:** `"quotes": ["error", "single", { "avoidEscape": true }]`
**Описание:** Требует одинарные кавычки, кроме случаев с экранированием.

```javascript
const str = "text"; // ❌ Error
const str = 'text'; // ✅ OK
const str = "can't"; // ✅ OK (avoidEscape)
```

### 8. `comma-dangle` - Trailing запятые

**Параметр:** `"comma-dangle": ["error", "never"]`
**Описание:** Запрещает trailing запятые в объектах/массивах.

```javascript
const arr = [1, 2, 3,]; // ❌ Error
const arr = [1, 2, 3]; // ✅ OK
```

### 9. `arrow-spacing` - Пробелы в стрелочных функциях

**Параметр:** `"arrow-spacing": ["error", { "before": true, "after": true }]`
**Описание:** Требует пробелы до и после `=>`.

```javascript
const fn = ()=>{}; // ❌ Error
const fn = () => {}; // ✅ OK
```

### 10. `indent` - Отступы

**Параметр:** `"indent": ["error", 2, { "SwitchCase": 1 }]`
**Описание:** Требует 2 пробела для отступов.

```javascript
function bad() {
    return true; // ❌ Error (4 пробела)
}

function good() {
  return true; // ✅ OK (2 пробела)
}
```

### 11. `no-multiple-empty-lines` - Пустые строки

**Параметр:** `"no-multiple-empty-lines": ["error", { "max": 1 }]`
**Описание:** Максимум 1 пустая строка подряд.

### 12. `space-before-blocks` - Пробел перед блоком

**Параметр:** `"space-before-blocks": "error"`
**Описание:** Требует пробел перед `{`.

```javascript
function bad(){ } // ❌ Error
function good() { } // ✅ OK
```

### 13. `space-in-parens` - Пробелы в скобках

**Параметр:** `"space-in-parens": ["error", "never"]`
**Описание:** Запрещает пробелы внутри `()`.

```javascript
const sum = ( 1 + 2 ); // ❌ Error
const sum = (1 + 2); // ✅ OK
```

### 14. `space-infix-ops` - Пробелы вокруг операторов

**Параметр:** `"space-infix-ops": "error"`
**Описание:** Требует пробелы вокруг операторов.

```javascript
const sum=1+2; // ❌ Error
const sum = 1 + 2; // ✅ OK
```

### 15. `no-debugger` - Запрет debugger

**Параметр:** `"no-debugger": "warn"`
**Описание:** Предупреждает о забытых `debugger` в коде.

```javascript
debugger; // ⚠️ Warning
```

## 📚 Примеры использования

### Базовый пример: До и После

**До (bad-code.js):**
```javascript
var userName = 'John'; // var вместо const
console.log(userName); // console.log
if (userName == 'John') { } // == вместо ===
```

**После (good-code.js):**
```javascript
const userName = 'John'; // const
// console.log удалён
if (userName === 'John') { } // ===
```

📁 Полные примеры в `examples/basic/`

### Продвинутые примеры

- **TypeScript** (`examples/advanced/typescript-example.ts`) - правила для TypeScript
- **React** (`examples/advanced/react-component.jsx`) - правила для React компонентов

## 🔧 Команды

### Проверка кода

```bash
# Проверить все файлы
npx eslint .

# Проверить конкретный файл
npx eslint examples/basic/bad-code.js

# Проверить с выводом результатов в консоль
npx eslint . --format=stylish
```

**Описание:** Анализирует код и выводит список ошибок и предупреждений.

### Автоматическое исправление

```bash
# Исправить все файлы
npx eslint . --fix

# Исправить конкретный файл
npx eslint examples/basic/bad-code.js --fix
```

**Описание:** Автоматически исправляет проблемы, которые можно исправить безопасно.

### Проверка с кэшированием

```bash
# Проверить только изменённые файлы
npx eslint . --cache
```

**Описание:** Кэширует результаты для ускорения повторных проверок.

### Инициализация конфигурации

```bash
# Интерактивная настройка ESLint
npx eslint --init
```

**Описание:** Создаёт `.eslintrc.json` с помощью интерактивного мастера.

## ⌨️ Горячие клавиши в VS Code

- **Fix all auto-fixable problems:** `Ctrl+Shift+P` → `ESLint: Fix all auto-fixable Problems`
- **Format on Save:** Включите в settings.json:
  ```json
  {
    "editor.codeActionsOnSave": {
      "source.fixAll.eslint": "explicit"
    }
  }
  ```

## 🔄 Интеграция с другими расширениями

### С Prettier

ESLint и Prettier отлично работают вместе:

```bash
npm install --save-dev eslint-config-prettier eslint-plugin-prettier
```

`.eslintrc.json`:
```json
{
  "extends": [
    "eslint:recommended",
    "plugin:prettier/recommended"
  ]
}
```

### С TypeScript

```bash
npm install --save-dev @typescript-eslint/parser @typescript-eslint/eslint-plugin
```

`.eslintrc.json`:
```json
{
  "parser": "@typescript-eslint/parser",
  "plugins": ["@typescript-eslint"],
  "extends": ["plugin:@typescript-eslint/recommended"]
}
```

### С React

```bash
npm install --save-dev eslint-plugin-react eslint-plugin-react-hooks
```

`.eslintrc.json`:
```json
{
  "plugins": ["react", "react-hooks"],
  "extends": [
    "plugin:react/recommended",
    "plugin:react-hooks/recommended"
  ]
}
```

## 📖 Популярные конфигурации

### Airbnb Style Guide

```bash
npx install-peerdeps --dev eslint-config-airbnb
```
Что это:
  - Один из самых популярных стандартов
  - 140k+ звёзд на GitHub
  - Очень подробный и продуманный

  Особенности:
  - 2 пробела для отступов
  - Всегда точки с запятой
  - Одинарные кавычки
  - Trailing запятые (в ES5+)
  - Строгие правила для React

  Пример:
  // Airbnb Style
  const myFunction = (param) => param * 2;

  const obj = {
    foo: 'bar',
    baz: 42,  // trailing comma
  };

```json
{
  "extends": "airbnb"
}
```

### Standard Style

```bash
npm install --save-dev eslint-config-standard
```

```json
{
  "extends": "standard"
}
```

### Google Style Guide

```bash
npm install --save-dev eslint-config-google
```
Что это:
  - Официальный стиль кодирования от Google
  - Используется в проектах Google (Angular, и др.)
  - Довольно строгий и консервативный

  Особенности:
  - 2 пробела для отступов
  - Всегда точки с запятой
  - Одинарные кавычки
  - Строгие правила для JSDoc комментариев

  Пример:
  // Google Style
  const myFunction = function(param) {
    return param * 2;
  };

```json
{
  "extends": "google"
}
```

## 🎯 Советы по использованию

1. **Включите Format on Save** - ESLint будет автоматически исправлять ошибки при сохранении
2. **Используйте .eslintignore** - исключите `node_modules/`, `dist/` и другие файлы
3. **Начните с recommended** - `eslint:recommended` - хорошая отправная точка
4. **Настраивайте под команду** - договоритесь о правилах с командой
5. **Интегрируйте в CI/CD** - проверяйте код перед merge

## 🔗 Полезные ссылки

- [Официальная документация ESLint](https://eslint.org/)
- [Список всех правил](https://eslint.org/docs/rules/)
- [ESLint VS Code расширение](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint)
- [Airbnb JavaScript Style Guide](https://github.com/airbnb/javascript)
- [ESLint Playground](https://eslint.org/play/)

---

[⬅️ Назад к списку расширений](../README.md)
